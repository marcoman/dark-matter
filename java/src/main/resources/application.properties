spring.application.name=dark-matter
server.port=${PORT:5000}
spring.thymeleaf.cache=true

# Session cookie (development default; override SECRET_KEY in production)
server.servlet.session.cookie.name=DARKMATTERSESSION
